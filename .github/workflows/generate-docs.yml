name: Update API Reference
on:
  workflow_dispatch:

jobs:
  generate_api_reference:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v2

      - name: Use Node ${{ matrix.node }}
        uses: actions/setup-node@v1
        with:
          node-version: ${{ matrix.node }}

      - name: yarn packages cache
        uses: actions/cache@v2
        with:
          path: |
            node_modules
            */*/node_modules
          key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
          restore-keys: |
            ${{ runner.os }}-yarn-

      - name: Install packages
        run: yarn install --frozen-lockfile
        
      - name: Build
        run: yarn build

      - name: Generate API Reference
        working-directory: ./packages/hms-video-store
        run: yarn docs
        
      - name: checkout 100ms-docs
        uses: actions/checkout@v2
        with:
          token: ${{ secrets.PAT_TOKEN }}
          repository: 100mslive/100ms-docs
          path: 100ms-docs
          
      - name: ls on root
        run: ls
          
      - name: go to 100ms-docs
        run: cd 100ms-docs
        
      - name: ls in 100ms-docs
        run: ls
        
      - name: ls in root again
        run: ls ..
        
      - name: ls in packages
        run: ls ../packages
        
      - name: ls in hms-video-store
        run: ls ../hms-video-store
        
      - name: ls in docs
        run: ls ../packages/hms-video-store/docs
        
      - name: copy over api reference
        run: cp -R ../packages/hms-video-store/docs docs/api-reference/javascript
        
      - name: delete earlier api reference
        run: rm -r v2
        
      - name: rename api reference folder
        run: mv docs v2

      - name: Create PR
        uses: peter-evans/create-pull-request@v3
        with:
          token: ${{ secrets.PAT_TOKEN }}
          commit-message: 'docs(web): update api reference'
          title: 'docs(web): update api reference'
          labels: automated pr
          branch: web-api-reference
          branch-suffix: timestamp
          base: main
          reviewers: triptu
          delete-branch: true
