import React from 'react';

const VideoHook = () => {
  return <></>;
};

const VideoStories = {
  title: 'Hooks/useAutoplayError',
  component: VideoHook,
};

export default VideoStories;

export const UseVideoHook = VideoHook.bind({});
UseVideoHook.storyName = 'useAutoplayError';
