{
  "installCommand": "echo \"https://x-access-token:${WHITEBOARD_PAT}@github.com\" >> ~/.git-credentials && git config --global credential.helper store && yarn install",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],
  "outputDirectory": "dist"
}
